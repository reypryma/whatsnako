import 'package:flutter/material.dart';

class ViewCallsFragment extends StatelessWidget {
  const ViewCallsFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Calls Frame"),
    );
  }
}
